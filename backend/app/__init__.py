"""Trove - claims the games that are temporarily free, on your own accounts."""

# Single source of truth for the app version. The release workflow derives image
# tags from the git tag and refuses to publish if the two disagree, so bump this
# and tag `v<version>` together.
__version__ = "0.1.4"
